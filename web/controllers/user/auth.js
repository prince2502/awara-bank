angular.module("userIndexModule").controller("AuthController", [ '$scope', 
	
	function($scope){
		
		$scope.logout = function(){
			// call a service to log user out
			// call another to relocate the user ... 
		}
		
	}
]);